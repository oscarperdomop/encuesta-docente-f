# 🏗️ Arquitectura del Sistema de Encuestas Docentes USCO

## 📊 Diagrama de Arquitectura General

```
┌─────────────────────────────────────────────────────────────────┐
│                         USUARIO FINAL                            │
│              (Estudiante / Docente / Jefe / Admin)              │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ HTTPS
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│                    FRONTEND (React + Vite)                       │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   Router     │  │   Zustand    │  │   Services   │         │
│  │  (7 rutas)   │  │   (Estado)   │  │   (API)      │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Components: Login, Intro, Docentes, Encuesta, Resumen  │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ REST API (JWT Bearer Token)
                         │ /api/v1/*
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│                    BACKEND (FastAPI)                             │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   Auth       │  │   Catalogs   │  │   Attempts   │         │
│  │   /auth/*    │  │   /surveys/* │  │   /attempts/*│         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   Sessions   │  │   Queue      │  │   Admin      │         │
│  │   /sessions/*│  │   /queue     │  │   /admin/*   │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  SQLModel + Alembic (Migraciones)                        │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ PostgreSQL Protocol
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│                    BASE DE DATOS (PostgreSQL)                    │
│                                                                  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐          │
│  │  users   │ │ teachers │ │ surveys  │ │ attempts │          │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘          │
│                                                                  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐          │
│  │  roles   │ │responses │ │  turnos  │ │  audit   │          │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘          │
└─────────────────────────────────────────────────────────────────┘
```

## 🔐 Flujo de Autenticación

```
┌─────────┐                ┌─────────┐                ┌─────────┐
│ Usuario │                │ Frontend│                │ Backend │
└────┬────┘                └────┬────┘                └────┬────┘
     │                          │                          │
     │ 1. Ingresa email         │                          │
     │─────────────────────────>│                          │
     │                          │                          │
     │                          │ 2. POST /auth/login      │
     │                          │─────────────────────────>│
     │                          │                          │
     │                          │    {email: "u2@usco..."}│
     │                          │                          │
     │                          │                          │ 3. Valida:
     │                          │                          │    ✓ @usco.edu.co
     │                          │                          │    ✓ Usuario existe
     │                          │                          │    ✓ Estado activo
     │                          │                          │    ✓ Turnos < límite
     │                          │                          │
     │                          │ 4. JWT Token             │
     │                          │<─────────────────────────│
     │                          │                          │
     │ 5. Guarda en localStorage│                          │
     │<─────────────────────────│                          │
     │                          │                          │
     │ 6. GET /auth/me          │                          │
     │                          │─────────────────────────>│
     │                          │  (Authorization: Bearer) │
     │                          │                          │
     │                          │ 7. Datos usuario         │
     │ 8. Redirige a /intro     │<─────────────────────────│
     │<─────────────────────────│                          │
```

## 📝 Flujo de Encuesta Completo

```
┌────────────────────────────────────────────────────────────────┐
│ 1. LOGIN                                                        │
│    POST /auth/login → JWT Token                                │
└────────────┬───────────────────────────────────────────────────┘
             ↓
┌────────────────────────────────────────────────────────────────┐
│ 2. PRESENTACIÓN & JUSTIFICACIÓN (Frontend, páginas locales)    │
└────────────┬───────────────────────────────────────────────────┘
             ↓
┌────────────────────────────────────────────────────────────────┐
│ 3. APERTURA DE TURNO                                           │
│    GET /sessions/turno/current (verificar si hay turno abierto)│
│    POST /sessions/turno/open (si no existe)                    │
│    → Guarda turno_id en sessionStorage                         │
└────────────┬───────────────────────────────────────────────────┘
             ↓
┌────────────────────────────────────────────────────────────────┐
│ 4. SELECCIÓN DE DOCENTES                                       │
│    GET /surveys/activas → Obtener encuesta activa              │
│    GET /surveys/{id}/teachers?include_state=true               │
│    → Usuario marca docentes (checkbox)                         │
│    POST /attempts {survey_id, teacher_ids[]}                   │
│    → Crea intentos para cada docente                           │
└────────────┬───────────────────────────────────────────────────┘
             ↓
┌────────────────────────────────────────────────────────────────┐
│ 5. OBTENER SIGUIENTE DOCENTE EN COLA                           │
│    GET /attempts/next?survey_id={id}                           │
│    → Devuelve attempt_id, teacher_nombre, expires_at           │
└────────────┬───────────────────────────────────────────────────┘
             ↓
┌────────────────────────────────────────────────────────────────┐
│ 6. ENCUESTA PASO 1 (Q1-Q9)                                     │
│    GET /surveys/{id}/questions → Lista de preguntas            │
│    → Usuario responde Q1-Q9 (Likert 1-5)                       │
│    PATCH /attempts/{attempt_id} (autosave cada cambio)         │
│    → Frontend navega a Paso 2                                  │
└────────────┬───────────────────────────────────────────────────┘
             ↓
┌────────────────────────────────────────────────────────────────┐
│ 7. ENCUESTA PASO 2 (Q10-Q16)                                   │
│    → Usuario responde Q10-Q15 (Likert 1-5)                     │
│    → Usuario responde Q16 (texto opcional, 3 campos)           │
│    PATCH /attempts/{attempt_id} (autosave)                     │
└────────────┬───────────────────────────────────────────────────┘
             ↓
┌────────────────────────────────────────────────────────────────┐
│ 8. ENVÍO FINAL                                                 │
│    POST /attempts/{attempt_id}/submit                          │
│    {                                                           │
│      answers: [{question_id, value}...],                      │
│      textos: {positivos, mejorar, comentarios}                │
│    }                                                           │
│    → Backend valida 15 respuestas Likert (Q1-Q15)             │
│    → Backend calcula scores por sección y total               │
│    → Estado cambia a "enviado"                                 │
└────────────┬───────────────────────────────────────────────────┘
             ↓
┌────────────────────────────────────────────────────────────────┐
│ 9. ¿MÁS DOCENTES EN COLA?                                      │
│    GET /attempts/next?survey_id={id}                           │
│    → Si hay más: repetir desde paso 6                          │
│    → Si no hay: ir a Resumen                                   │
└────────────┬───────────────────────────────────────────────────┘
             ↓
┌────────────────────────────────────────────────────────────────┐
│ 10. RESUMEN DE TURNO                                           │
│    GET /attempts/summary?survey_id={id}                        │
│    → Muestra: enviados, en_progreso, pendientes                │
│    → Habilita "Finalizar turno" solo si todos "enviado"       │
└────────────┬───────────────────────────────────────────────────┘
             ↓
┌────────────────────────────────────────────────────────────────┐
│ 11. CIERRE DE TURNO                                            │
│    POST /sessions/turno/close (header X-Turno-Id)             │
│    → Backend cierra turno y actualiza estado                   │
│    → Frontend limpia localStorage y sessionStorage             │
│    → Redirige a /login                                         │
└────────────────────────────────────────────────────────────────┘
```

## 🗂️ Modelo de Datos (Entidades Principales)

```
┌─────────────┐         ┌─────────────┐         ┌─────────────┐
│    users    │         │   roles     │         │  teachers   │
├─────────────┤         ├─────────────┤         ├─────────────┤
│ id (UUID)   │         │ id (UUID)   │         │ id (UUID)   │
│ email       │◄────┐   │ nombre      │         │ identificador│
│ nombre      │     │   │ descripcion │         │ nombre      │
│ estado      │     │   └─────────────┘         │ programa    │
└─────────────┘     │                           │ estado      │
                    │                           └─────────────┘
                    │                                  △
                    │                                  │
              ┌─────────────┐                         │
              │ user_roles  │                         │
              ├─────────────┤                         │
              │ user_id     │─────────────────────────┤
              │ role_id     │                         │
              └─────────────┘                         │
                                                      │
┌─────────────┐         ┌─────────────┐              │
│   surveys   │         │  questions  │              │
├─────────────┤         ├─────────────┤              │
│ id (UUID)   │         │ id (UUID)   │              │
│ codigo      │         │ codigo      │              │
│ nombre      │◄────┐   │ enunciado   │              │
│ estado      │     │   │ tipo        │              │
│ fecha_inicio│     │   │ peso        │              │
│ fecha_fin   │     │   │ orden       │              │
└─────────────┘     │   │ section_id  │              │
       △            │   └─────────────┘              │
       │            │                                │
       │      ┌─────────────────────┐                │
       │      │ survey_teacher_     │                │
       └──────┤   assignments       │────────────────┘
              ├─────────────────────┤
              │ survey_id           │
              │ teacher_id          │
              └─────────────────────┘
                       △
                       │
              ┌─────────────┐
              │  attempts   │
              ├─────────────┤
              │ id (UUID)   │
              │ survey_id   │─────────────────────┐
              │ user_id     │                     │
              │ teacher_id  │                     │
              │ estado      │ (en_progreso /      │
              │ intento_nro │  enviado /          │
              │ expires_at  │  expirado /         │
              └─────────────┘  fallido)           │
                     △                            │
                     │                            │
              ┌─────────────┐                     │
              │  responses  │                     │
              ├─────────────┤                     │
              │ id (UUID)   │                     │
              │ attempt_id  │─────────────────────┘
              │ question_id │
              │ value       │ (1-5 para Likert)
              │ texto       │ (JSONB para Q16)
              └─────────────┘

┌─────────────┐         ┌─────────────┐
│   turnos    │         │ audit_logs  │
├─────────────┤         ├─────────────┤
│ id (UUID)   │         │ id (UUID)   │
│ user_id     │         │ user_id     │
│ status      │         │ action      │
│ opened_at   │         │ ip_address  │
│ closed_at   │         │ user_agent  │
└─────────────┘         │ timestamp   │
                        └─────────────┘
```

## 🔄 Máquina de Estados de Attempts

```
                    ┌──────────────┐
                    │   INICIAL    │
                    │  (no existe) │
                    └──────┬───────┘
                           │
                           │ POST /attempts
                           ↓
                    ┌──────────────┐
              ┌────►│ en_progreso  │
              │     └──────┬───────┘
              │            │
              │            │ 30 min sin submit
              │            ├──────────────────────┐
              │            │                      ↓
   PATCH      │            │              ┌──────────────┐
   (autosave) │            │              │   expirado   │
              │            │              └──────────────┘
              │            │
              │            │ POST /attempts/{id}/submit
              │            ├──────────────────────┐
              │            │                      ↓
              └────────────┤              ┌──────────────┐
                           │              │   enviado    │ (FINAL)
                           │              └──────────────┘
                           │
                           │ Abandono / Error
                           ↓
                    ┌──────────────┐
                    │   fallido    │
                    └──────────────┘

Bloqueo: (expirado + fallido) ≥ 2 → Usuario bloqueado
Admin puede otorgar +1 intento extra en attempt_limits
```

## 📦 Stack Tecnológico Detallado

### Frontend
```
React 19.1.1              → UI Library
TypeScript 5.8.3          → Type Safety
Vite 7.1.7               → Build Tool & Dev Server
React Router DOM 7.9.3   → Client-side Routing
Zustand 5.0.8            → State Management (lighter than Redux)
Axios 1.12.2             → HTTP Client
TailwindCSS 3.4.17       → Utility-first CSS
```

### Backend
```
FastAPI 0.104+           → Web Framework
SQLModel 0.0.14+         → ORM (SQLAlchemy + Pydantic)
Alembic 1.12+            → Database Migrations
PostgreSQL               → Database
Uvicorn                  → ASGI Server
PyJWT 2.8                → JWT Token Management
Passlib                  → Password Hashing
Pandas 2.1+              → Data Processing (reportes)
OpenPyXL 3.1+            → Excel Export
```

## 🚀 Despliegue

### Frontend (Vercel)
```
vercel.json configurado
Build: npm run build
Output: dist/
Env vars: VITE_API_URL
```

### Backend (Render / Railway / Fly.io)
```
Dockerfile (recomendado)
o
requirements.txt + uvicorn
Env vars: DATABASE_URL, JWT_SECRET
```

### Base de Datos (Supabase / Render PostgreSQL)
```
PostgreSQL 14+
Extensiones: pgcrypto (para gen_random_uuid())
```

## 🔐 Seguridad

### Implementado
- ✅ JWT Bearer Token Authentication
- ✅ Validación de dominio @usco.edu.co
- ✅ Rate limiting por intentos (2 max)
- ✅ Expiración de sesiones (30 min)
- ✅ CORS configurado
- ✅ Headers de seguridad

### Pendiente / Recomendado
- ⚠️ HTTPS en producción
- ⚠️ Logging estructurado
- ⚠️ Monitoreo y alertas
- ⚠️ Backup automatizado de BD
- ⚠️ WAF (Web Application Firewall)

---

**Última actualización**: 2025-11-10  
**Versión**: 1.0.0
